# COPY THIS FILE TO secrets.yml AND FILL IN YOUR VALUES

# The password for the new sudo user created on the server
new_user_password: "CHANGE_ME_TO_A_STRONG_PASSWORD"

# The secret string used to verify GitHub webhooks
webhook_secret: "CHANGE_ME_TO_A_RANDOM_STRING"

# (Optional) GitHub Personal Access Token for commit status updates
# Scope required: repo:status
github_pat: "" 

# Your Public SSH Key (cat ~/.ssh/id_rsa.pub)
new_user_ssh_key: "ssh-rsa AAA..."
